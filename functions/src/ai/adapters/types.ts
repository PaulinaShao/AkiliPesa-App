export type VendorResult = { outputUrl?: string | null; error?: string | null; meta?: any };
export type VendorPayload = { input: string; options?: Record<string, any>; uid: string; requestId: string };
